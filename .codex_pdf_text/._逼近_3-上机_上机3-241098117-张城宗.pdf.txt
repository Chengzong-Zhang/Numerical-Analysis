1   题目一 学号：241098117，姓名：张城宗                                                               1



                       数值分析第三周上机作业
                            学号：241098117 姓名：张城宗


§1      题目一：f (x) = ex 在 [0, 1] 上的最佳平方逼近

1.1    问题
   求函数 f (x) = ex 在区间 [0, 1] 上的 n 次最佳平方逼近多项式，分别考察以下四种情形：
 1. n = 5，基函数取 ϕk (x) = xk ；
 2. n = 5，基函数取 ϕk (x) = Pk (2x − 1)；
 3. n = 10，基函数取 ϕk (x) = xk ；
 4. n = 10，基函数取 ϕk (x) = Pk (2x − 1)。
要求比较不同基函数下的逼近误差和数值稳定性。

1.2    算法思路
      根据教材第 3.4 节，若 Φn+1 = span{ϕ0 , ϕ1 , . . . , ϕn }，则最佳平方逼近函数 ϕ∗ (x) 满足

                                  ∥f − ϕ∗ ∥22 = min ∥f − ϕ∥22 .
                                                    ϕ∈Φn+1


令
                                                    ∑
                                                    n
                                         ϕ(x) =             ci ϕi (x),
                                                      i=0

则由极值条件可得法方程组

                         ∑
                         n
                               ci (ϕi , ϕj ) = (f, ϕj ),           j = 0, 1, . . . , n,
                         i=0


其中                                              ∫ 1
                                     (u, v) =          u(x)v(x) dx.
                                                  0

      当取幂函数基 ϕk (x) = xk 时，
                                                 ∫ 1
                                                                           1
                          Gij = (xi , xj ) =           xi+j dx =               ,
                                                  0                      i+j+1

因此 Gram 矩阵是 Hilbert 矩阵。教材指出该矩阵高阶时高度病态，所以虽然逼近空间相同，但数值
求解容易放大舍入误差。
  当取 Legendre 基 ϕk (x) = Pk (2x−1) 时，由于 Legendre 多项式在 [−1, 1] 上关于权函数 W (x) = 1
正交，线性映射到 [0, 1] 后，Gram 矩阵为对角阵。于是系数可直接由

                                                       (f, ϕk )
                                             ck =
                                                      (ϕk , ϕk )

计算，数值稳定性明显优于幂函数基。
1   题目一 学号：241098117，姓名：张城宗                                                              2


1.3    结果分析
      实验结果如表 1 与图 1 所示。

                                      表 1: 题目一结果对比
         编号       情形                     cond(G)          L2 误差           最大误差
            (1)   n = 5，幂函数基           1.4951 × 107    6.9353 × 10−7    2.5983 × 10−6
            (2)   n = 5，Legendre 基           1         6.9382 × 10−7    2.5983 × 10−6
            (3)   n = 10，幂函数基          5.2232 × 1014   1.5195 × 10−10   6.7049 × 10−10
            (4)   n = 10，Legendre 基          1         2.1073 × 10−8    6.0396 × 10−14




                             图 1: 题目一四种最佳平方逼近结果

     从图像上看，四条逼近曲线都与精确函数基本重合，说明在平滑函数 ex 上，多项式最佳平方逼近
效果很好。但从条件数看，两类基函数差别极大。幂函数基下，Gram 矩阵条件数从 107 迅速上升到
1014 ，病态性非常明显；而 Legendre 基下条件数始终为 1，求解过程稳定得多。
     另外，理论上不同基函数张成的是同一个多项式空间，因此最佳平方逼近结果应一致。实验中各
数值略有差异，主要来自数值求解法方程时的舍入误差，而不是逼近理论本身的差异。

1.4    结论
  本题说明：在相同的逼近空间中，基函数的选取直接影响数值稳定性。幂函数基虽然形式简单，但
对应 Hilbert 矩阵，病态性强；Legendre 正交基可以把 Gram 矩阵化为对角阵，明显提高求解稳定性。
2   题目二 学号：241098117，姓名：张城宗                                                                3


因此在实际计算中，应优先选用正交基来构造最佳平方逼近。


§2     题目二：例 3.5.3 数据的最小二乘拟合

2.1    问题
      利用教材例 3.5.3 中的 10 组数据

              x =(0.000, 0.895, 1.641, 2.512, 3.542, 4.054, 4.602, 5.063, 5.354, 5.617),
              y =(1.000, 1.803, 3.680, 7.320, 13.59, 17.41, 22.19, 24.89, 26.55, 29.77),

完成以下两部分实验：
 1. 在多项式空间 Pn 中做最小二乘拟合，取 n = 1, 2, 3, 4；
 2. 将基函数改为 {1, x, cos x, sin x}，比较拟合效果。

2.2    算法思路
      教材第 3.5.1 节指出，线性最小二乘拟合可转化为误差函数

                                           1 ∑(             )2
                                                 m
                                    L(c) =      ϕ(xi ) − yi
                                           m
                                                 i=1


的极小化问题。若
                                                 ∑
                                                 n
                                        ϕ(x) =         ck ϕk (x),
                                                 k=0

构造设计矩阵
                                          Aik = ϕk−1 (xi ),

则法方程组为
                                          (AT A)c = AT y.

  对于多项式拟合，设计矩阵是 Vandermonde 型矩阵，次数越高，矩阵条件数通常越大。对于一般
基函数组 {1, x, cos x, sin x}，只要在数据点上满足 Haar 条件，最小二乘解依然唯一存在。本题程序直
接使用 MATLAB 的左除运算符 A\y 求解，它内部基于 QR 分解，较法方程更稳定。

2.3    结果分析
      实验结果如表 2 与图 2、图 3 所示。


                                    表 2: 题目二拟合结果对比
                     拟合方法              cond(A)         RMS 误差       均方误差 L
                     P1 拟合           8.1998 × 100        2.155425    4.645856
                     P2 拟合           6.2211 × 101        0.553638    0.306515
                     P3 拟合           5.4645 × 102        0.375963    0.141348
                     P4 拟合           5.7835 × 103        0.365702    0.133738
                     混合基拟合           1.3830 × 101        0.529299    0.280158
2   题目二 学号：241098117，姓名：张城宗                                     4




                  图 2: 题目二中多项式空间 Pn 的拟合结果




                  图 3: 题目二中各方法曲线与 RMS 误差比较

  从结果看，随着多项式次数从 1 次提高到 4 次，拟合误差明显下降，说明高阶多项式对数据整体
趋势的刻画更充分。但与此同时，条件数也从 100 增长到 103 量级，说明模型越复杂，数值稳定性越
差。
  混合基 {1, x, cos x, sin x} 的 RMS 误差为 0.529299，优于一次拟合，也略优于二次拟合，但不如
三次和四次多项式。这说明加入三角项后模型表达能力增强，但对这组单调上升的数据来说，多项式
3   题目三 学号：241098117，姓名：张城宗                                                                 5


高次项的效果仍更好。

2.4    结论
  本题说明最小二乘拟合需要同时考虑精度和稳定性。提高多项式次数确实可以减小残差，但也会
增大条件数。对本题数据而言，四次多项式拟合效果最好；若兼顾模型复杂度和稳定性，则三次多项
式或混合基拟合也具有一定合理性。


§3     题目三：9 次多项式拟合与条件数分析

3.1    问题
   仍采用例 3.5.3 的 10 组数据，令多项式次数 n = 9，此时参数个数 n + 1 = 10 恰与数据点个数相
同。要求：
 1. 计算幂函数基下 9 次多项式拟合的条件数和均方误差；
 2. 解释理论上 RMS 应为 0，而计算结果却不是 0 的原因；
 3. 将基函数改为 Chebyshev 多项式基，比较条件数和误差变化。

3.2    算法思路
             最小二乘问题退化为方阵线性系统求解问题。若数据点互不相同，
  当 m = n+1 时，                               则 Vandermonde
矩阵可逆，因此 9 次多项式拟合实际上等价于 9 次 Lagrange 插值。理论上应有

                                ϕ∗ (xi ) = yi ,   i = 1, 2, . . . , 10,

故残差应为零，RMS 也应为零。
  但幂函数基产生的 Vandermonde 矩阵高度病态，小的舍入误差会被条件数放大，因此实际计算会
出现非零残差。为改善这一问题，本题引入 Chebyshev 多项式基

                             Tk (t) = cos(k arccos t),      t ∈ [−1, 1],

并将原区间线性映射到 [−1, 1]。由于 Chebyshev 基更接近正交结构，设计矩阵条件数会显著降低。

3.3    结果分析
      结果如表 3 与图 4 所示。


                                   表 3: 题目三两种基函数的比较
            基函数                         cond(A)            cond(AT A)        RMS 误差
            幂函数基 {xk }              2.983236 × 1010      3.419494 × 1020   8.4409 × 10−11
            Chebyshev 基 {Tk (t)}    1.072485 × 102       1.150225 × 104    4.6428 × 10−15
4   题目四 学号：241098117，姓名：张城宗                                                      6




                     图 4: 题目三中幂函数基与 Chebyshev 基的比较

  由图表可以看出，两种基函数下的拟合曲线都几乎通过全部数据点，这与“m = n + 1 时等价于插
值”的理论结论一致。但从数值层面看，幂函数基下的条件数高达 1010 量级，法方程矩阵更达到 1020
量级，是严重病态问题，因此计算得到的 RMS 仅能接近 0，而不能真正等于 0。
  改用 Chebyshev 基后，设计矩阵条件数降低到 102 量级，RMS 下降到 10−15 量级，几乎达到机
器精度。这说明非零残差的根本原因不是模型错误，而是幂函数基下矩阵病态导致舍入误差被放大。

3.4    结论
  本题表明：理论上成立的精确插值，在数值计算中仍可能因病态矩阵而出现明显误差。改变基函
数是改善稳定性的有效手段。与幂函数基相比，Chebyshev 基显著降低了条件数，使拟合结果更接近
理论零误差。


§4      题目四：FFT 算法实现与信号压缩

4.1    问题
      本题包括三个部分：
    1. 自行实现 Cooley–Tukey 基-2 FFT 算法，并与 MATLAB 内置 fft 对比；
    2. 利用 FFT 对合成音频信号进行频域压缩；
    3. 利用二维 FFT 对合成图像进行压缩。

4.2    算法思路
      教材第 3.5.2 节给出离散 Fourier 变换

                             ∑
                             N −1
                    X[k] =          x[n]e−2πikn/N ,   k = 0, 1, . . . , N − 1,
                             n=0


及其逆变换
                                              N −1
                                            1 ∑
                                   x[n] =       X[k]e2πikn/N .
                                            N
                                              k=0
4   题目四 学号：241098117，姓名：张城宗                                                    7


若直接按定义计算全部频域系数，复杂度为 O(N 2 )。FFT 的核心思想是把 N 点 DFT 分解为两个 N /2
点 DFT，即
             X[k] = E[k] + WNk O[k],
             X[k + N /2] = E[k] − WNk O[k],      k = 0, 1, . . . , N /2 − 1,

其中 WNk = e−2πik/N 。这样总复杂度可降为 O(N log2 N )。
   在压缩应用中，若信号能量集中在少数频率分量，只保留幅值最大的若干 Fourier 系数，再用逆
FFT 重建，就能实现有损压缩。保留比例越高，重建越接近原信号；保留比例越低，压缩率更高，但
失真也更明显。

4.3   结果分析
  1. FFT 正确性验证：对长度 N = 64 的复数测试信号，自实现 FFT 与 MATLAB 内置 fft 的
最大误差为
                         1.0658 × 10−14 ,

说明算法实现正确。
  2. 音频信号压缩：主要频率分量和压缩结果见表 4、表 5，图 5 给出了重建效果。


                       表 4: 题目四音频信号主要频率分量
                            序号     频率 (Hz)       幅度
                              1      49.80      941.20
                              2     120.12      484.57
                              3      50.78      246.34
                              4     200.20      237.16
                              5      48.83      151.66



                          表 5: 题目四音频信号压缩结果
                  保留比例       系数个数 K           RMS 误差         SNR
                    20%           205          0.1641      19.9 dB
                    5%            51           0.2273      17.1 dB
                    1%            10           0.3903      12.4 dB
4   题目四 学号：241098117，姓名：张城宗                     8




              图 5: 题目四音频信号在不同比例频域截断下的重建效果

  从图像可以看出，保留 20% 系数时重建波形与原信号较接近；保留 5% 时主频结构仍在，但细节
开始损失；仅保留 1% 时失真已经比较明显。这说明该信号的大部分能量集中在少数频率成分上，FFT
压缩是有效的。
  3. 图像压缩：结果如表 6 与图 6 所示。


                      表 6: 题目四图像压缩结果
                   保留比例   保留系数个数    图像 SNR
                    10%       410   24.6 dB
                    2%        82    23.3 dB
4   题目四 学号：241098117，姓名：张城宗                                 9




                   图 6: 题目四二维 FFT 图像压缩效果

  二维频谱图显示，图像能量集中在少数低频和特征频率附近。即使只保留 2% 的频率系数，图像
整体纹理仍然较清晰；保留 10% 时图像更接近原图，SNR 也更高。

4.4   结论
  本题说明 FFT 不仅在理论上能把 DFT 的复杂度由 O(N 2 ) 降到 O(N log N )，而且在实际应用中
也是高效的频域分析与压缩工具。无论是一维音频信号还是二维图像，只要频谱能量具有集中性，就
可以通过保留主要频率系数实现较好的压缩效果。
5       附录 学号：241098117，姓名：张城宗                                                                      10



§5        附录：程序代码

5.1      题目一关键代码


                       Listing 1: 最佳平方逼近中幂函数基与 Legendre 基的核心实现
    1   f = @ ( x ) exp ( x ) ;
    2   f_L2sq = ( exp (2) - 1) / 2;
    3   x_plot = linspace (0 , 1 , 500) ;
    4   y_exact = f ( x_plot ) ;
    5

    6   for n = [5 , 10]
    7       G = hilb ( n + 1) ;
    8       f_vec = zeros ( n + 1 , 1) ;
    9       for j = 0: n
10              f_vec ( j + 1) = integral ( @ ( x ) exp ( x ) .* x .^ j , 0 , 1) ;
11          end
12          c_mono = G \ f_vec ;
13          kappa_G = cond ( G ) ;
14

15            phi_mono = zeros ( size ( x_plot ) ) ;
16            for k = 0: n
17                 phi_mono = phi_mono + c_mono ( k + 1) * x_plot .^ k ;
18            end
19            L2_err_mono = sqrt ( integral ( @ ( x ) ( exp ( x ) - polyval ( flipud ( c_mono ) ,
                  x ) ) .^2 , 0 , 1) ) ;
20

21            c_leg = zeros ( n + 1 , 1) ;
22            for k = 0: n
23                inner_fk = integral ( @ ( x ) exp ( x ) .* legendre_on_01 (k , x ) , 0 , 1) ;
24                c_leg ( k + 1) = (2 * k + 1) * inner_fk ;
25            end
26            phi_leg = zeros ( size ( x_plot ) ) ;
27            for k = 0: n
28                phi_leg = phi_leg + c_leg ( k + 1) * legendre_on_01 (k , x_plot ) ;
29            end
30            max_err_leg = max ( abs ( y_exact - phi_leg ) ) ;
31      end

        算例结果：n = 10 时，幂函数基对应 cond(G) = 5.2232 × 1014 ，而 Legendre 基对应 cond(G) = 1。

5.2      题目二关键代码


                        Listing 2: 最小二乘拟合的设计矩阵构造、求解与误差计算
    1   x_data = [0.000 , 0.895 , 1.641 , 2.512 , 3.542 , 4.054 , 4.602 , 5.063 ,
           5.354 , 5.617];
    2   y_data = [1.000 , 1.803 , 3.680 , 7.320 , 13.59 , 17.41 , 22.19 , 24.89 ,
           26.55 , 29.77];
    3   x_plot = linspace ( min ( x_data ) , max ( x_data ) , 500) ;
    4
5       附录 学号：241098117，姓名：张城宗                                                                     11


    5   for n = [1 , 2 , 3 , 4]
    6       A = zeros ( length ( x_data ) , n + 1) ;
    7       for k = 0: n
    8           A (: , k + 1) = x_data (:) .^ k ;
    9       end
10          c = A \ y_data (:) ;
11          y_fit = A * c ;
12          rms_err = sqrt ( mean (( y_fit - y_data (:) ) .^2) ) ;
13          kappa_A = cond ( A ) ;
14

15            A_plot = zeros ( length ( x_plot ) , n + 1) ;
16            for k = 0: n
17                A_plot (: , k + 1) = x_plot (:) .^ k ;
18            end
19            fit_poly = A_plot * c ;
20      end
21

22      A_mix = [ ones ( length ( x_data ) , 1) , x_data (:) , cos ( x_data (:) ) , sin ( x_data
           (:) ) ];
23      c_mix = A_mix \ y_data (:) ;
24      y_fit_mix = A_mix * c_mix ;
25      rms_mix = sqrt ( mean (( y_fit_mix - y_data (:) ) .^2) ) ;

        算例结果：四次多项式拟合的 RMS 误差为 0.365702，混合基拟合的 RMS 误差为 0.529299。

5.3      题目三关键代码


                     Listing 3: 9 次多项式拟合中 Chebyshev 基与幂函数基的对比实现
    1   m = length ( x_data ) ;
    2   n = m - 1;
    3

    4   A_mono = zeros (m , n + 1) ;
    5   for k = 0: n
    6       A_mono (: , k + 1) = x_data (:) .^ k ;
    7   end
    8

    9   a = min ( x_data ) ;
10      b = max ( x_data ) ;
11      t_data = (2 * x_data - ( a + b ) ) / ( b - a ) ;
12

13      A_cheby = zeros (m , n + 1) ;
14      for k = 0: n
15          A_cheby (: , k + 1) = cos ( k * acos ( t_data (:) ) ) ;
16      end
17

18      c_mono = A_mono \ y_data (:) ;
19      c_cheby = A_cheby \ y_data (:) ;
20      kappa_mono = cond ( A_mono ) ;
21      kappa_cheby = cond ( A_cheby ) ;
22
5       附录 学号：241098117，姓名：张城宗                                                    12


23      y_fit_mono = A_mono * c_mono ;
24      y_fit_cheby = A_cheby * c_cheby ;
25      rms_mono = sqrt ( mean (( y_fit_mono - y_data (:) ) .^2) ) ;
26      rms_cheby = sqrt ( mean (( y_fit_cheby - y_data (:) ) .^2) ) ;

        算例结果：幂函数基下 cond(A) = 2.983236 × 1010 ，Chebyshev 基下 cond(A) = 1.072485 × 102 。

5.4      题目四关键代码


                           Listing 4: 基-2 FFT 递归实现与频域压缩核心代码
    1   function X = my_fft ( x )
    2       N = length ( x ) ;
    3       if N == 1
    4           X = x;
    5           return ;
    6       end
    7       x_even = x (1:2: end ) ;
    8       x_odd = x (2:2: end ) ;
    9       E = my_fft ( x_even ) ;
10          O = my_fft ( x_odd ) ;
11          k = 0 : N / 2 - 1;
12          W = exp ( -2 i * pi * k / N ) ;
13          X = [ E + W .* O , E - W .* O ];
14      end
15

16      Fs = 1000;
17      t = 0 : 1 / Fs    :   1 - 1 / Fs ;
18      x_signal = 2.0    *   sin (2 * pi * 50 * t ) ...
19               + 1.0    *   sin (2 * pi * 120 * t ) ...
20               + 0.5    *   sin (2 * pi * 200 * t ) ...
21               + 0.2    *   randn ( size ( t ) ) ;
22

23      N_sig = length ( t ) ;
24      N_fft = 2^ nextpow2 ( N_sig ) ;
25      x_padded = [ x_signal , zeros (1 , N_fft - N_sig ) ];
26      X_freq = my_fft ( x_padded ) ;
27

28      ratio = 0.20;
29      K = round ( ratio * N_fft ) ;
30      [~ , idx_sorted ] = sort ( abs ( X_freq ) , ’ descend ’) ;
31      X_compressed = zeros ( size ( X_freq ) ) ;
32      X_compressed ( idx_sorted (1: K ) ) = X_freq ( idx_sorted (1: K ) ) ;
33

34      x_rec = real ( my_ifft ( X_compressed ) ) ;
35      x_rec = x_rec (1: N_sig ) ;
36      rms_err = sqrt ( mean (( x_rec - x_signal ) .^2) ) ;
37      snr_db = 20 * log10 ( norm ( x_signal ) / norm ( x_rec - x_signal ) ) ;

  算例结果：自实现 FFT 与 MATLAB 内置 fft 的最大误差为 1.0658 × 10−14 ；音频信号保留 20%
频率系数时，SNR 为 19.9 dB。
